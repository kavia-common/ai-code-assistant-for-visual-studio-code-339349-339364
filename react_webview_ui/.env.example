# React Webview UI (browser/dev mode) -> Node backend REST base URL
# In VS Code webview, calls should be proxied by the extension host; this is for direct smoke testing.
REACT_APP_NODE_BACKEND_URL=http://localhost:3001
